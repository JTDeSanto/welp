
<div class="row">
<div id="carouselExampleControls" class="carousel slide" data-ride="carousel">
  <div class="carousel-inner" role="listbox">
    <div class="carousel-item active">
      <%= image_tag 'slider1.jpg', class: 'd-block img-fluid' %>
       <div class="carousel-caption d-none d-md-block">
      <h3>Get the best eats in town</h3>
    </div>
    </div>
    <div class="carousel-item">
      <%= image_tag 'slider2.jpg', class: 'd-block img-fluid' %>
       <div class="carousel-caption d-none d-md-block">
      <h3>Find the best restaurants on vacation</h3>
    </div>
    </div>
    <div class="carousel-item">
      <%= image_tag 'slider3.jpg', class: 'd-block img-fluid' %>
       <div class="carousel-caption d-none d-md-block">
      <h3>Discover the latest hot spots</h3>
    </div>
    </div>
  </div>
  <a class="carousel-control-prev" href="#carouselExampleControls" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleControls" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
</div>
</div>



<% @places.each do |place| %>
<div class="booyah-box col-10 offset-1">
  <h1><%= link_to place.name, place_path(place)%></h1>

  <div class="row">
    <div class="col-6">
    <i><%=  place.address %></i><br /><br />
    <p><%=  place.description %> </p><br />

    <% if place.user.present? %>
      <small><p>Created by:<%= place.user.email %></p></small>
    <% end %>
    </div>

    <div class="col-6">
      <div class="row">

        <% place.photos.each do |photo| %>
          <% if photo.picture.present? %>
          <div class="col-5">
            <%= image_tag photo.picture, class: 'img-thumbnail' %>
          </div>
          <% end %>
        <% end %>
      </div>
    </div>

  </div>

  <div class="clear"></div>

  <% if place.comments.present? %>
    <div class="col-12">
      <blockquote>
        <% if place.comments.last.humanized_rating.present? %>
          <b><%= place.comments.last.humanized_rating %> </b>
          <% end %>
        <%= place.comments.last.message %>
        <br />
        <small><%= place.comments.last.user.email %></small>
      </blockquote>
    </div>
  <% end %>
</div>




<% end %>
<div class="flickr_pagination col-6 offset-4">
<%= will_paginate @places %>
</div>